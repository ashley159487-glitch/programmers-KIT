def solution(arr, queries):
    
    for s, e, k in queries :
        for j in range(s, e + 1 ) :
            if j % k == 0 :
                arr[j] += 1
    return arr

어제부터 계속 날 괴롭히던 수열과 구간쿼리문제가 또 나왔다.
어제 문제와 초반 설정은 같기에 반복문 안에 다시 반복문을 넣어주고
만족하는 식을 넣어준다.
여기까진 그래도 막힘이 없었는데 그 반복문에서 나온 값을
굳이 리스트형 변수에 집어넣고 그걸 return시키려니 자꾸 값을 틀리게 됐다.
그리고 계산만 하고 그걸 다시 저장하지 않아 값이 반영되지 않았다...

"arr의 값을 직접 바꿔라" 유형 (지금 푸신 문제)
핵심: arr[i] = ... 처럼 인덱스에 직접 대입합니다.
리턴: 처음에 받은 arr를 그대로 리턴합니다.
"새로운 배열을 만들어라" 유형 (예: n보다 큰 수들만 골라라)
핵심: answer = []를 만들고 answer.append()를 사용합니다.
리턴: 새로 만든 answer를 리턴합니다.
"누적된 하나의 값을 구해라" 유형 (예: 배열의 총합)
핵심: total = 0 같은 변수를 하나 만들고 total += i를 사용합니다.
리턴: 계산된 total 변수 하나만 리턴합니다.

제미나이가 말해준 이것들을 잘 기억하자.
